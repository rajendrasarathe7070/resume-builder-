# resume-builder-
ResumeHub – Create, share and edit resumes with public/private links. Pure HTML/CSS/JS, no backend, localStorage persistence. Each resume gets two links: public view (read-only) and private edit link (full control).
